export default {
  theme: {
    extend: {
      fontFamily: {
        sans: ["Inter", "sans-serif"], // default text
        mono: ['"JetBrains Mono"', "monospace"], // numbers / code
      },
    },
  },
};
